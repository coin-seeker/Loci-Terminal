services:
  lociterm:
    build: .
    ports:
      - "127.0.0.1:${LOCITERM_PORT:-8080}:8080"
    volumes:
      - lociterm-data:/data
      - lociterm-home:/home/lociterm
    restart: unless-stopped

volumes:
  lociterm-data:
  lociterm-home:
